{% extends 'base.html.twig' %}

{% block title %}Offre{% endblock %}

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offre.id }}</td>
            </tr>
            <tr>
                <th>OffreAcheteur</th>
                <td>{{ offre.OffreAcheteur }}</td>
            </tr>
            <tr>
                <th>DateOffre</th>
                <td>{{ offre.dateOffre ? offre.dateOffre|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>HeureOffre</th>
                <td>{{ offre.heureOffre ? offre.heureOffre|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offre_index') }}">back to list</a>

    <a href="{{ path('offre_edit', {'id': offre.id}) }}">edit</a>

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
